# agora-relay configuration
# Copy this file to .env and adjust as needed.
# CLI arguments override these values.
# Peer public keys can also be managed in ~/.agora-relay/peers.json

# Port the relay listens on (default: 9470)
AGORA_PORT=9470

# Host/interface to bind to (default: 0.0.0.0)
AGORA_HOST=0.0.0.0

# Directory to persist messages for offline storage-enabled peers
# Supports ~/ expansion (default: ~/.agora-relay/storage)
AGORA_STORAGE_DIR=~/.agora-relay/storage

# Comma-separated public keys to enable offline message storage for
# These are combined with keys listed in ~/.agora-relay/peers.json
AGORA_STORAGE_PEERS=
